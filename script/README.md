# Script 

This folder contains files to ensure good quality of the code.

There is only one useful file for this project: the `install.sh' file. It install the different library locally. 

Use `script/install.sh` to run the code (from parent directory)
